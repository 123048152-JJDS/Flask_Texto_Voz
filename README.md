# Flask_Texto_Voz


Aplicación web con Flask que convierte texto en audio (MP3) usando gTTS (Google Text-to-Speech).

python -m venv .venv
.venv\Scripts\activate
python.exe -m pip install --upgrade pip
pip install -r requirements.txt